### Rules

Use UV and ruff with pre-commit config. To setup project for the first time:
```
uv init
```

To add dependencies:
```
uv add <dep_name>
```

### Style

We use Django REST Framework, in `apps/` style, all apps in that folder. Create 5 logical apps.

### Schema

Schema is located in @docs/uicdev.drawio.